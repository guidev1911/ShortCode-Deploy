server:
  port: 8888
spring:
  application:
    name: encurtadorURL
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
  level:
    root: INFO
    # root: WARN
spring-doc:
  swagger-ui:
    use-root-path: true